import "../styles/carousel.css";

export default function Carousel() {
	return (
		<section className="carousel-shell" aria-label="Featured games">
			<div className="carousel-heading">
				<h2 className="carousel-title">Juegos destacados</h2>
				<p className="carousel-copy">Una selección inicial para la home mientras se arma el carrusel real.</p>
			</div>
		</section>
	);
}
